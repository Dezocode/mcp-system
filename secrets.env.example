# Example Secrets Template
# Copy this file and replace with your actual secret values

# Database Password (PostgreSQL)
# Requirements: 
# - At least 12 characters
# - Mix of letters, numbers, and special characters
# - Avoid common passwords
DB_PASSWORD="change_this_to_secure_password_123!"

# Grafana Admin Password
# Requirements:
# - At least 8 characters
# - Used to access Grafana dashboard at /grafana
GRAFANA_PASSWORD="change_this_grafana_password_456!"

# JWT Secret Key
# Requirements:
# - At least 32 bytes for security
# - Used for authentication tokens
# - Can be generated with: openssl rand -base64 32
JWT_SECRET_KEY="change_this_jwt_secret_key_or_generate_with_openssl"

# Instructions:
# 1. Copy this file: cp secrets.env.example secrets/secrets.env
# 2. Edit secrets/secrets.env with your actual values
# 3. Run: source secrets/secrets.env && ./scripts/setup-secrets.sh
# 4. Verify: ls -la secrets/
# 5. Remove the source file: rm secrets/secrets.env (for security)

# Example secure password generation:
# DB_PASSWORD=$(openssl rand -base64 24)
# GRAFANA_PASSWORD=$(openssl rand -base64 16)
# JWT_SECRET_KEY=$(openssl rand -base64 32)